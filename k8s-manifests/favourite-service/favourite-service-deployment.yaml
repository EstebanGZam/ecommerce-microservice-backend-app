apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
    kompose.cmd: kompose convert -f compose.yml -o k8s-manifests/
    kompose.version: 1.36.0 (ae2a39403)
  labels:
    io.kompose.service: favourite-service
  name: favourite-service
  namespace: ecommerce-ns
spec:
  replicas: 1
  selector:
    matchLabels:
      io.kompose.service: favourite-service
  template:
    metadata:
      labels:
        io.kompose.service: favourite-service
    spec:
      automountServiceAccountToken: false
      containers:
        - env:
            - name: SPRING_CONFIG_IMPORT
              value: optional:configserver:http://cloud-config.ecommerce-ns.svc.cluster.local:9296/
          envFrom:
            - configMapRef:
                name: common-environment-variables
          image: selimhorri/favourite-service-ecommerce-boot:0.1.0
          name: favourite-service
          ports:
            - containerPort: 8800
              protocol: TCP
          resources:
            requests:
              memory: "384Mi"
              cpu: "300m"
              ephemeral-storage: "1Gi"
            limits:
              memory: "768Mi"
              cpu: "750m"
              ephemeral-storage: "2Gi"
      restartPolicy: Always 
